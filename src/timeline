#!/bin/env python
from configparser import ConfigParser
import subprocess
import argparse


def main() -> None:
    config = ConfigParser(allow_no_value=False, empty_lines_in_values=False)
    config.optionxform = lambda option: option
    config.read('.code/timeline.config')
    parser = argparse.ArgumentParser()
    parser.add_argument(
        'file', nargs='?', default=config.get('timeline', 'MainFile',
                                              fallback='timeline.tln')
    )
    args = parser.parse_args()
    subprocess.run(f'nvim -u .code/nvim.config {args.file}', shell=True)


if __name__ == '__main__':
    main()
